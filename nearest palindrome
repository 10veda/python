#PF-Assgn-46

def nearest_palindrome(number):
    f=True
    
    while(f):
        number+= 1
        s=str(number)
        w=s[::-1]
        if s==w:
            f=False
    return number
         
        
number=12300
print(nearest_palindrome(number))
